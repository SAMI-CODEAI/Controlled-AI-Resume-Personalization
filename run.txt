# 🚀 How to Run the Platform (MERN Stack)

## 💻 Manual Setup (Local Development)

### 1. Database Setup
Ensure you have **MongoDB** installed and running on your local machine (Default: `mongodb://localhost:27017`).

### 2. Backend (Node.js / Express)
1. Navigate to the `backend` folder:
   ```bash
   cd backend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Ensure your `.env` file is configured (copy from `.env.example` if applicable).
4. Start the backend:
   ```bash
   npm start
   # OR for development (with auto-reload):
   npm run dev
   ```
5. The API will be available at: http://localhost:8000

### 3. Frontend (React / Next.js)
1. Open a new terminal and navigate to the `frontend` folder:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the development server:
   ```bash
   npm run dev
   ```
4. Access the frontend at: http://localhost:3000

---

## 🔑 Environment Configuration

### Backend (`backend/.env`):
- `PORT` (Default: `8000`)
- `MONGODB_URI` (e.g., `mongodb://localhost:27017/resumedb`)
- `JWT_SECRET_KEY` (Your secret key)
- `OPENAI_API_KEY` (Your OpenAI Key)
- `LLM_BASE_URL` (Optional: e.g., Ollama or custom endpoint)

### Frontend (`frontend/.env`):
- `NEXT_PUBLIC_API_URL` (Default: `http://localhost:8000`)

---

## 🐳 Option 2: Using Docker
(Ensure you update the `Dockerfile` and `docker-compose.yml` if you decide to containerize the MERN stack).
1. Run:
   ```bash
   docker-compose up --build
   ```

